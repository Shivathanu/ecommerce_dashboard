import React from "react";
import Layout from "../core/Layout";

const Signin = () => (
    <Layout title="Signin page" description="Sign in to the page">
        ...
    </Layout>
);

export default Signin;